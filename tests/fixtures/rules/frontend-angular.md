---
paths:
  - "**/*.component.ts"
  - "**/*.directive.ts"
description: fixture rule, used only by test-render.sh
---

Fixture rule body. Content is arbitrary — the test suite only checks
frontmatter shape and provenance, never this text.
