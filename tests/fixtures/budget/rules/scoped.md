---
paths:
  - "**/*.component.ts"
description: scoped — must never appear in the always-on budget report
---

Fixture rule body. Content is arbitrary — the test suite only checks that
this rule never appears in the report at all.
