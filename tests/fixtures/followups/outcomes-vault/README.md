Fixture vault for the follow-up outcome vocabulary — kept separate from
repos-vault so an outcome case added here never shifts the counts that vault's
grouping tests assert, which has already cost two rounds of updates elsewhere.

Every item is tagged `#repo/`, so attribution is exact and these tests are about
outcomes and nothing else.
