# 2026-01-04

## Built
- `alpha-service`: same barcode work, still not shipped.
- `beta-app`: a second stream, so context names two repos and must not guess —
  which is what leaves the untagged item below genuinely unattributed.

## Follow-ups
- [ ] Merge barcode PR #28 and ship a TestFlight build (`1.1.0+24` on `feature/barcode`) #repo/alpha-service
- [ ] Device retest: real hit plus a Greek miss into the label photo flow #repo/alpha-service
- [ ] Create the app record on the developer console and set `appId` in `config.json` for the release build (needs an API key) #repo/beta-app
- [ ] Merge barcode PR #28 and ship a store build #repo/beta-app
- [ ] Rotate the staging deploy key before the next release

## Drift / gaps
- none
